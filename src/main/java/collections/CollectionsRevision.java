package collections;

public class CollectionsRevision {
    // Create an array : of size 10, with some values

    // Print array elements, length

    // Populate array with a value

    // Create list from array

    // Sort array, binary search in array

    // Create an arraylist, get and set values

    // List
    // Create array list

    // Test           int size(),
    //                boolean isEmpty(),
    //                boolean contains(Object element),
    //                boolean add(E element),
    //                boolean remove(Object element), and
    //                Iterator<E> iterator().

    //                containsAll, addAll, removeAll



    // TO PRACTICE
    // Comparator sort - Arrays, List

    // Arrays.sort(testArray, Comparator.comparingInt((int[] o) -> o[0]));

    // Array to list, list to array
    // Map looping

    // Using streams







}
